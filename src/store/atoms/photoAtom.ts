import { atom } from "recoil";

export const photoCarouselAtom = atom({
    key: "photoCarouselAtom",
    default: null
});

export const photoModalAtom = atom({
    key: "photoModalAtom",
    default: false,
})

